<Page x:Class="SmartTuningSystem.View.Welcome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Welcome" >
    <Grid>
        <!--<Grid Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            -->
        <!-- 单元格 (0,0) -->
        <!--
            <Grid Grid.Row="0" Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                -->
        <!-- 嵌套的子单元格 -->
        <!--
                <Border Grid.Row="0" Grid.Column="0" Margin="10" >
                    <Border.Background>
                        <ImageBrush ImageSource="../Assets/Images/pic12.jpg" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>

                <Border Grid.Row="0" Grid.Column="1" Margin="10" >
                    <Border.Background>
                        <ImageBrush ImageSource="../Assets/Images/pic12.jpg" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>
            </Grid>



            -->
        <!-- 单元格 (0,1) -->
        <!--
            <Border Grid.Row="1" Grid.Column="0" Margin="10">
                <Label Content="欢迎进入JABIL-CNC智能调机系统" VerticalAlignment="Center" HorizontalAlignment="Center" FontStyle="Italic" FontWeight="Bold" FontSize="36"/>
            </Border>

        </Grid>-->
        <Grid.Background>
            <ImageBrush ImageSource="../Assets/Images/pic4.jpg" Stretch="UniformToFill"/>
        </Grid.Background>
        <Label Content="欢迎进入JABIL" HorizontalAlignment="Center" FontStyle="Italic" FontWeight="Bold" FontSize="48" Foreground="{DynamicResource SkinOppositeColor}"/>
        <Label Content="CNC智能调机系统" HorizontalAlignment="Center" FontStyle="Italic" FontWeight="Bold" FontSize="48" Foreground="{DynamicResource SkinOppositeColor}" VerticalAlignment="Bottom"/>
    </Grid>

    <!--<Border BorderThickness="0,1,1,1"  BorderBrush="Transparent" >
        <Grid>
            --><!--<Grid Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            -->
            <!-- 单元格 (0,0) -->
            <!--
            <Grid Grid.Row="0" Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                -->
            <!-- 嵌套的子单元格 -->
            <!--
                <Border Grid.Row="0" Grid.Column="0" Margin="10" >
                    <Border.Background>
                        <ImageBrush ImageSource="../Assets/Images/pic12.jpg" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>

                <Border Grid.Row="0" Grid.Column="1" Margin="10" >
                    <Border.Background>
                        <ImageBrush ImageSource="../Assets/Images/pic12.jpg" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>
            </Grid>



            -->
            <!-- 单元格 (0,1) -->
            <!--
            <Border Grid.Row="1" Grid.Column="0" Margin="10">
                <Label Content="欢迎进入JABIL-CNC智能调机系统" VerticalAlignment="Center" HorizontalAlignment="Center" FontStyle="Italic" FontWeight="Bold" FontSize="36"/>
            </Border>

        </Grid>--><!--
            <Grid.Background>
                <ImageBrush ImageSource="../Assets/Images/pic4.jpg" Stretch="UniformToFill"/>
            </Grid.Background>
            <Label Content="欢迎进入JABIL" HorizontalAlignment="Center" FontStyle="Italic" FontWeight="Bold" FontSize="48" Foreground="{DynamicResource SkinOppositeColor}"/>
            <Label Content="CNC智能调机系统" HorizontalAlignment="Center" FontStyle="Italic" FontWeight="Bold" FontSize="48" Foreground="{DynamicResource SkinOppositeColor}" VerticalAlignment="Bottom"/>
        </Grid>
    </Border>-->

</Page>
